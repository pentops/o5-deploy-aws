
---

registry:
  owner: pentops
  name: o5-deploy

options:
  subPackages:
    - name: service
    - name: topic
    - name: events

packages:
  - label: AWS Deployer
    name: o5.aws.deployer.v1

dependencies:
  - bufRegistry:
      owner: bufbuild
      name: protovalidate

  - bufRegistry:
      owner: googleapis
      name: googleapis

  - registry:
      owner: pentops
      name: j5

  - registry:
      owner: pentops
      name: messaging
